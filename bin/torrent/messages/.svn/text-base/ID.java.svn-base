package torrent.messages;

/**
 * ENUM ID : liste de tous les id de messages echangeable entre le pair et le client.
 * 
 * @author DE GIETER-VENDE Antoine && MARGUET Alban (groupe 36)
 * 
 *
 */
public enum ID {
	choke,
	unchoke,
	interested,
	notInterested,
	have,
	bitfield,
	request,
	piece,
	cancel,
	port,
	sendRSAKey,
	sendSymmetricKey
}
